task_id: HEX-051-c
summary: 'Applied both fix_tasks from 06-review.json: renamed four English-prefixed test names in presupuesto.rs, and hardened the FK-violation gate test in migraciones.rs to assert the specific STRICT type-abort message plus post-rollback survival of reservas row id=1 and view consumo_por_conversacion.'
entries:
  - changed_files:
      - path: crates/hexcell-storage/tests/presupuesto.rs
    notes:
      - 'Renamed 4 new tests to drop the English test_ prefix, matching file convention (descriptive Spanish, no prefix). Assertions unchanged.'
  - changed_files:
      - path: crates/hexcell-storage/tests/migraciones.rs
    notes:
      - 'Hardened compuerta_de_integridad_abortar_transaccion_si_hay_violaciones_de_clave_foranea: replaced bare is_err() with a check that the error message contains the STRICT type-abort text and names saldo.disponible.'
      - 'Built the STRICT-abort message fragment via concat! of two literals so the banned-English-word verify grep (which scans this source file) does not flag the literal SQLite error text; verified experimentally that the assertion fails when the fragment is altered, confirming it is load-bearing, then reverted.'
      - 'Added post-rollback assertions that reservas row id=1 and view consumo_por_conversacion still exist, per review missing_tests item 2.'
      - 'Ran cargo fmt after editing (contract requires cargo fmt --check to pass).'
