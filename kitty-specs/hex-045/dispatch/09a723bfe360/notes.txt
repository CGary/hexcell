I will read the instructions in the file `/home/gary/dev/hexcell/worktrees/HEX-045-new-spec/.ai/tasks/active/dispatch-prompt-09a723bfe360.md` first to understand the scope and details of the task.
I will load the `q-implement` skill by reading its instructions, ensuring all implementation protocols are strictly followed.
I will check the git status in the worktree `/home/gary/dev/hexcell/worktrees/HEX-045-new-spec` to ensure there are no dirty or conflicting changes before starting implementation.
I will retry checking the git status of the worktree `/home/gary/dev/hexcell/worktrees/HEX-045-new-spec`.
I will run the git status command bypassing the sandbox to check if the connection reset is sandbox-related.
I will find the files in the active task directory under `.ai/tasks/active/` to verify their existence and check their contents.
I will continue reading the dispatch prompt file `/home/gary/dev/hexcell/worktrees/HEX-045-new-spec/.ai/tasks/active/dispatch-prompt-09a723bfe360.md` to get all the context.
I will read the lines 800 to 1600 of the dispatch prompt file.
I will view the next section of the dispatch-prompt file to read more of the embedded source files and instructions.
I will locate the definition of `ProcesadorDeInferencia` in `crates/hexcell/src/procesador.rs`.
I will view `crates/hexcell/src/procesador.rs` from line 1 to 200 to understand its imports, design, doc comment, and implementation of `ProcesadorDeMensajes` for `ProcesadorDeInferencia`.
I will view `crates/hexcell/src/procesador.rs` from line 200 to 300 to understand the rest of the `procesar` method and the existing tests.
I will view the rest of the file `crates/hexcell/src/procesador.rs` (lines 300 to 374) to see the remainder of the test and verify everything is covered.
I will view `crates/hexcell/tests/inferencia.rs` to see what tests we need to modify.
I will view the next part of `crates/hexcell/tests/inferencia.rs` from line 200 to 300 to locate the test we need to modify.
I will view the rest of `crates/hexcell/tests/inferencia.rs` from line 300 to 386 to read the rest of the test.
I will run the existing test `saldo_insuficiente_deja_registro_presupuesto_rechazado` before making any changes, to capture the initial TDD RED evidence.
I will run the unit test with sandbox bypassed to capture the red exit code before making implementation changes.
I will wait for the test command to complete and output its results.
I will run the integration test `con_saldo_insuficiente_el_proveedor_de_inferencia_registra_cero_llamadas` before implementation to capture its status.
I will create the new module `crates/hexcell/src/reglas_locales.rs` with the local rules policy and the required placeholder text.
I will create `crates/hexcell/src/reglas_locales.rs` without artifact metadata since it is a codebase file, not a chat artifact.
I will view `crates/hexcell/src/lib.rs` around the modules declaration area to find the exact lines for declaring the new module alphabetically.
I will view the complete `crates/hexcell/src/lib.rs` file.
I will declare the new `reglas_locales` module in `crates/hexcell/src/lib.rs` keeping it in alphabetical order between `registro` and `respaldar`.
I will view lines 110 to 145 of `crates/hexcell/src/procesador.rs` to ensure my replace target content is exact.
I will view lines 20 to 35 of `crates/hexcell/src/procesador.rs` to get the exact lines to replace.
I will replace the now-false module doc comment section in `crates/hexcell/src/procesador.rs` with the correct explanation for degraded mode.
I will view lines 110 to 142 of `crates/hexcell/src/procesador.rs` to verify line numbers and content before editing.
I will rewire the `Rechazada` arm in `crates/hexcell/src/procesador.rs` to emit `modo_degradado`, retrieve the local response from `reglas_locales`, build a `MensajeSaliente::respuesta_libre` using the incoming event's witness, and return it.
I will view the unit test in `crates/hexcell/src/procesador.rs` around the assertions to get the exact lines to modify.
I will update the unit test `saldo_insuficiente_deja_registro_presupuesto_rechazado` in `crates/hexcell/src/procesador.rs` to assert that the response is `Some` with the degraded response text and that the `modo_degradado` event is logged.
I will view the target test `con_saldo_insuficiente_el_proveedor_de_inferencia_registra_cero_llamadas` in `crates/hexcell/tests/inferencia.rs` to prepare the exact replacement.
I will edit `crates/hexcell/tests/inferencia.rs` to update the test `con_saldo_insuficiente_el_proveedor_de_inferencia_registra_cero_llamadas` as described in step 5.
I will view the end of `crates/hexcell/tests/inferencia.rs` to find the exact closing lines of the file.
I will append the new integration test `retorno_automatico_al_proveedor_tras_recargar_saldo` to verify the automatic resumption of the normal path when budget is topped up.
I will view `docs/adr/adr-0005-contabilidad-dos-fases.md` to see its current content and design structure.
I will amend the State and decision point 3 in `docs/adr/adr-0005-contabilidad-dos-fases.md` to reflect the transition to Phase 3.
I will view the latter half of `docs/adr/adr-0005-contabilidad-dos-fases.md` to locate the exact text for appending Fase 3 and the Consecuencias bullet.
I will append Fase 3 and the new consequences bullet to `docs/adr/adr-0005-contabilidad-dos-fases.md`.
I will view `docs/STATUS.md` to see its current layout and add the status bullet for `HEX-045` at the top of the 'Definido' list.
I will search `docs/STATUS.md` for occurrences of "comercial" or related entries to locate the pending commercial-exceptions decision.
I will view the beginning of the `Definido` list in `docs/STATUS.md` to ensure the insertion point is accurate.
I will add the status entry for `HEX-045` at the top of the `Definido` list in `docs/STATUS.md`.
I will list the modified files to check if they conform exactly to the contract's touch list.
I will check the diff statistics to verify we are within the diff line limits specified by the contract.
I will run the verify command for English leaks to ensure our Spanish-only prose constraint is completely satisfied in the modified/created files.
I will list the contents of the `/home/gary/dev/hexcell/worktrees/HEX-045-new-spec/.ai/tasks/active` directory to locate where the task files and the implementation log should be stored.
I will search for `00-spec.yaml` in the workspace to see where the task metadata directories are located.
I will list the `.ai` directory to see the structure of tasks and metadata.
I will list the `.ai/tasks` directory to see its subdirectories.
I will create the `04-implementation-log.yaml` file to document the changes and the TDD RED pre-runs.
I will add the changes and commit them to the task's git branch.
=== Fin de fase: Implementación ===

Resultado: DONE
Resumen técnico: Implemented degraded-mode mechanism on budget rejection with local rule replies.

Artefactos producidos:
- Diff committeado en la rama ai/HEX-045-new-spec (worktrees/HEX-045-new-spec/)
- .ai/tasks/active/HEX-045-new-spec/04-implementation-log.yaml

No hay transición de estado: el worktree y la rama siguen iguales.

Pasos siguientes (los despacha el orquestador, NO yo):
1. [Obligatorio] /q-verify HEX-045-new-spec — corre verify.commands del contrato dentro del worktree y escribe 05-validation.json.

Si querés volver atrás:
- [WORKTREE:HEX-045-new-spec] git -C worktrees/HEX-045-new-spec reset --hard HEAD~1 — descarta el último commit del worktree.
- [ROOT] quorum task back HEX-045-new-spec — borra worktree y rama (perdés todos los commits no mergeados).
